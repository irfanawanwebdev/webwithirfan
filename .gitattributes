# Normalize line endings: store as LF in the repo, check out native on each OS.
* text=auto eol=lf

# Treat binary assets as binary (no EOL conversion / no diffs).
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.ico  binary
*.woff  binary
*.woff2 binary
