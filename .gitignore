# Dependencies
node_modules

# Build output (the host rebuilds this from source — don't commit it)
dist
dist-ssr
dist-server
*.local

# Environment / secrets (only .env.example is committed)
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript incremental build info
*.tsbuildinfo

# Deploy tooling
.vercel
.netlify

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Original design handoff/prototype — reference only, not part of the build (being relocated out of the repo)
design_handoff_webwithirfan/
